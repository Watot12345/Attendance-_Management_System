/**
 * Awards JS — awards.js
 * Period picker toggle, calculate trigger
 */

function showAwardResults() {
  const results = document.getElementById('award-results');
  if (results) {
    results.scrollIntoView({ behavior: 'smooth' });
  }
}
